06/08/26

# Completed
- Set up controller/sprite manager

# Partly Completed
//storing the images in memory is much faster than loading them from disk every time.
done through BitmapImage walk2 = LoadImage("Walk2.png");

can check how many files ar ein a folder through 

Directory.GetFiles().Length()
    string[] files = Directory.GetFiles(
        folderPath,
        animationName + "*.png"
    );

var walkAnimation = LoadAnimation(
    @"Sprites\Player",
    "Walk"
);

controller
private BitmapImage[] _currentAnimation;
private int _currentFrame;

private void AnimationTick(object? sender, EventArgs e)
{
    _currentFrame = (_currentFrame + 1) % _currentAnimation.Length;
    image.Source = _currentAnimation[_currentFrame];
}

# Notes for later/Want to complete next

1.Controller (pet basics) decides animation
2.SpriteManager returns Animation object
3.Controlle advances frame every tick
4.Controller renders current frame